# 验证记录

## 2026-06-18：原子题库恢复与可恢复 OCR 队列

- `.qbank` 桌面恢复改为单一 SQLite 事务；题库元数据或任一题目无效时整体回滚，不留下半恢复题库。
- OCR 支持一次选择多张图片或扫描 PDF；PDF 在前端逐页渲染，每页识别前先写入本地内容寻址附件仓库。
- 队列只保存附件 ID、页名、状态与已完成 Markdown；应用重启后运行中页面回到等待状态，已完成页面不重复调用模型。
- 支持完成当前页后暂停、取消全部未完成页面、重试失败/取消页面；运行中的 GLM 请求使用后端取消令牌，本地 Tesseract 通过终止 worker 取消。
- 全量验证：前端 22 个测试文件、82 项通过；Rust 36 项通过；TypeScript/Vite 生产构建、`cargo fmt --check`、`cargo check` 与 `git diff --check` 通过。
- 自动化测试未调用外部 OCR Provider，未消耗测试 API 额度。

## 2026-06-18：自测总分与 AI 批次恢复

- 自测结果顶部始终展示当前累计总分与试卷满分；主观题待评分时明确提示数量，评分返回后实时更新总分。
- 每道题在结果卡顶部显示“小题得分 / 小题满分”，尚未评分时显示待评分状态。
- AI 批量解析按题库保存 Provider、风格、并发数和剩余题目检查点；暂停、失败或应用重启后可继续剩余批次。
- 已成功写入解析的题目会从检查点移除，恢复时还会与当前缺失解析题目重新核对，避免重复付费生成。
- 全量验证：前端 21 个测试文件、79 项通过，TypeScript/Vite 生产构建和 `git diff --check` 通过。

## 2026-06-18：键盘焦点与任务状态播报

- 为按钮、选择器、输入框和可聚焦自定义控件增加统一 `:focus-visible` 焦点环。
- OCR 进度使用标准 `progressbar` 语义并暴露当前百分比，错误使用即时警报语义。
- AI 批量解析进度使用礼貌播报，逐项失败可被辅助技术即时识别。
- AI 批量任务保持“完成当前请求后暂停”；已成功解析逐题落库，重新进入后可按剩余缺失题继续。

## 2026-06-18：Provider 删除、命名试卷与当前模型提示

- Provider 删除覆盖 SQLite 配置与操作系统凭据，重复删除保持幂等。
- 自定义组卷可按题库命名保存多套试卷，支持载入、更新和显式删除；已保存试卷与最近答题工作区互不覆盖。
- 侧栏状态显示当前默认启用语言模型的模型名；在设置页保存或删除配置后即时刷新。
- 浏览器验证侧栏在未配置 Provider 时显示“未配置模型”，不再误导为固定“本地模式”。
- 全量验证：前端 18 个测试文件、72 项通过；Rust 34 项通过；生产构建、`cargo fmt --check` 与 `cargo check` 均通过。

## 2026-06-18：OCR 附件仓库与可恢复导入草稿

- SQLite 迁移版本升级至 3；`assets` 保存去重内容元数据，`asset_links` 独立保存来源、派生角色、Provider 与模型关系。
- 原图、OCR 原始 JSON 和 Markdown 按 SHA-256 写入应用数据目录；同内容复用文件，IPC 不暴露磁盘路径。
- GLM-OCR 与本地 Tesseract 均保存三类附件；附件保存失败不会丢弃已经取得的 OCR 结果。
- OCR 结果可进入统一校正工作台，草稿编辑过程自动恢复；导入默认新建命名题库，也可明确选择追加到已有题库。
- 全量验证：前端 16 个测试文件、67 项通过；Rust 33 项通过；`cargo fmt --check`、`cargo check`、TypeScript/Vite 生产构建和 `git diff --check` 均通过。
- 未调用外部 OCR Provider，未消耗测试 API 额度。

## 2026-06-18：架构拆分与 AI 主观题评分闭环

- 新增架构测试：页面不超过 300 行、组件不超过 250 行、domain 禁止依赖 React/Tauri。
- 将刷题题目交互从 `PracticePage` 拆入 `features/practice/components`，页面只保留流程编排。
- Provider 仓储已从 Rust 数据库门面拆出，门面由 910 行降至 824 行；后续数据库能力继续按聚合拆分。
- 新增 AI 评分草案校验、自动计分和会话总分合并领域逻辑；合法评分返回后立即采用建议分。
- 新增独立 AI 评分服务、Provider/模型选择、自动触发、失败重试、逐评分点反馈及可选人工调分。
- 已采用的评分详情同时持久化到答题工作区和 SQLite `ai_grading_json`；迁移版本升级至 2，旧会话仍可读取。
- 将自测题目卡、结果页与 AI 评分面板拆入 feature 组件；`PracticePage` 114 行，`TestPage` 200 行。
- 自动化验证：前端 13 个测试文件、59 项通过；Rust 25 项通过；`cargo fmt --check`、`cargo check`、生产构建和 `git diff --check` 均通过。
- 未消耗测试 API 额度；真实 Provider 网络调用与桌面端人工交互仍需单独验收。

## 2026-06-18：自定义组卷与答题工作区持久化

- 刷题和自测首次进入时可按题号范围、题型数量组卷，并可精确勾选题目、上下调整题序。
- 两种模式按 `mode + bankId` 保存独立工作区，包括试卷、题序、当前位置、答案、确认状态和参考答案显示状态。
- 普通跳题、切换页面和应用刷新不清空答案；仅“清空答案”和确认“换卷”执行清理。
- 自动化验证：9 个测试文件、47 项测试通过，TypeScript 与 Vite 构建通过。
- 浏览器验证：刷题答题后立即切页可恢复，刷新并重新打开题库仍保留判题结果；自测切页后恢复两道已答状态。
- 浏览器控制台无错误或警告。

## 2026-06-18：答题中题型与题序导航

- 刷题与自测侧栏共享题型筛选和题号直达面板，并显示当前题、已答题状态。
- 两种模式支持顺序/乱序；切换顺序时保留当前题。
- 自测持久化题目顺序、顺序模式和当前位置，重新进入后可恢复。
- 自动化验证：前端 7 个测试文件、40 项测试通过，TypeScript 与 Vite 构建通过。
- 浏览器验证：题号跳转、题型筛选、乱序切换、自测乱序恢复均通过。

## 2026-06-18：DOCX/PDF 导入批次

- DOCX：从 OOXML 正文按段落和表格顺序提取文字，进入现有规则切题与人工校正工作台。
- PDF：逐页提取文本并把页码传递到 `DocumentBlock` 和题目来源范围。
- 扫描型 PDF：没有可提取文字时返回 OCR 分流提示，不生成空题库。
- 损坏的 DOCX/PDF 会被明确拒绝。
- 使用真实生成并视觉核对的中文 DOCX、文本 PDF、无文字 PDF 作为回归 fixture。
- 当前限制：DOCX 的可靠分页信息无法从 OOXML 正文直接恢复，因此按连续文本显示；浏览器开发模式仍只读取 TXT/Markdown，DOCX/PDF 使用桌面端 Rust 解析。

## 2026-06-18：自测闭环批次

- 新增自测会话领域评分：客观题统一计分，未答单独统计，主观题保持待批改状态。
- 新增 SQLite/Tauri 与浏览器 localStorage 同构会话存储。
- 自测作答自动保存，重新进入同一题库可恢复当前题号、作答及显示答案状态。
- 统一提交后显示正确、错误、未答、待批改数量，并支持只看错题和未答。
- 本地浏览器流程验证：示例题库进入自测、作答、翻页、刷新后重新进入并恢复到第 2 题，已答数量保持为 1/2。
- 自动化验证：前端 6 个测试文件共 34 项通过；Rust 共 17 项测试通过；生产构建通过。

最近一次验证：2026-06-17

## v0.2.0 本轮实际验证

当前交付环境已执行：

### 前端与共享逻辑

- `npm install`：通过。
- `npx tsc -b`：通过（由 `npm run build` 执行）。
- `npx vitest run`：通过，共 4 个测试文件、27 项测试。
- `npm run build`：通过，Vite 生产构建成功，共转换 346 个模块。
- SourcePreview 样式冲突静态检查：通过，不再把 `DocumentBlock.kind="option"` 直接输出为全局 `.option` class。

测试覆盖包括：

- 题目评分核心；
- TXT/Markdown 规则切题与导入校验；
- 导入草稿 reducer；
- AI Provider 预设基本完整性。

### Rust / Tauri 后端

当前交付容器没有安装 Rust/Cargo，因此 v0.2.0 新增的 AI 命令和 HTTP 适配器**未在本环境执行**：

- `cargo fmt --check`：未执行；
- `cargo check`：未执行；
- `cargo test`：未执行；
- `npm run tauri:dev`：未执行。

v0.1.2 基线此前已在用户 Windows 开发机完成 `cargo check`、`cargo test` 与 Tauri 窗口启动验证；但这不能替代 v0.2.0 新增 Rust 文件的重新编译，因此合并后仍必须在 Windows 本机执行下方命令。

## Windows 本机验收命令

```powershell
cd "D:\项目\quiz_studio_foundation"

npm install
npx tsc -b
npx vitest run
npm run build

powershell -NoProfile -ExecutionPolicy Bypass -File ".\src-tauri\run-check.ps1"
powershell -NoProfile -ExecutionPolicy Bypass -File ".\src-tauri\run-test.ps1"
powershell -NoProfile -ExecutionPolicy Bypass -File ".\src-tauri\run-dev.ps1"
```

## 手工验收清单

### 原始文档预览

1. 导入包含多道选择题的 Markdown。
2. 确认选项行不再逐字堆叠或互相覆盖。
3. 点击题干、选项、答案等任意属于该题的原文行，右侧定位到对应题目。
4. 缩放窗口并验证窄屏下原文面板仍正常换行。

### AI 解析

1. 在设置页选择厂商预设。
2. 填写真实模型名和 API Key，保存配置。
3. 点击“测试连接”。
4. 打开一个含缺失解析题目的题库。
5. 先执行单题生成，确认解析立即写入并可展开查看。
6. 执行批量生成，测试 1～4 并发和“完成当前请求后暂停”。
7. 重启应用，确认已经生成的解析仍保存在 SQLite。
8. 检查数学 LaTeX 和化学 `mhchem` 内容能正常渲染。

## 已知边界

- 当前仅支持 OpenAI Compatible 与 Anthropic Messages 两类文本模型协议；Gemini 使用其 OpenAI 兼容入口。
- 批量任务不会覆盖已有解析。
- 已成功题目会逐题持久化，但本版尚未建立独立的批量任务表，因此应用重启后不会恢复进度面板；重新点击时会继续扫描仍为空的题目。
- AI 输出会尝试解析结构化 JSON；若厂商忽略格式要求，则保存其纯文本/Markdown 内容。
- 模型生成内容可能存在事实错误，重要题库仍需人工抽查。
- Windows 安装包（`tauri build`）尚未在本轮构建。
