# AI 自动评分交互设计

自测提交后，主观题仍先显示为待批改。用户点击“AI 评分”并获得有效评分草稿时，系统立即采用模型建议分、更新总分并持久化，无需再次确认。模型调用或解析失败时不修改成绩。

评分结果显示分数、反馈、评分点、模型与耗时。用户可选择重新评分，也可修改分数并保存调整；这两项都是评分后的可选操作。领域模型使用 `SubjectiveGrade` 和 `gradedAt` 表达已采用评分，不再使用 `Confirmed` 命名。

范围只包括评分领域、评分面板、结果页与会话类型引用；后端评分草稿接口和 `ai_grading_json` 数据库字段保持兼容。
