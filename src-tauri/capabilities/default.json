{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for Quiz Studio",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
